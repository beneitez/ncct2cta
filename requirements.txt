# Core pipeline (works on Apple MPS locally and CUDA on cloud).
# Install torch separately per platform if needed:
#   local (M1):  pip install torch torchvision        # MPS wheels
#   cloud (CUDA): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
torchvision>=0.17
monai>=1.3
nibabel>=5.2
numpy>=1.24
scipy>=1.10
scikit-image>=0.22
matplotlib>=3.7
tqdm>=4.66
einops>=0.7
PyYAML>=6.0
pandas>=2.0

# Stage 3 (hybrid loss) extras — optional
pytorch-msssim>=1.0
lpips>=0.1.4
